# Новые возможности v1.2.0

**Дата:** 2026-01-03

Это руководство объясняет новые функции приложения YouTube Downloader версии 1.2.x.

---

## 🔄 Dual mode: Python + CLI

### Что это?

Приложение использует **два режима** для получения информации о видео:

| Режим | Как работает | Когда используется |
|-------|-------------|-------------------|
| **Python** | `python3 -m yt_dlp` | Для YouTube (лучше обходит защиту) |
| **CLI** | `/opt/homebrew/bin/yt-dlp` | Для других сайтов (быстрее) |

### Почему это важно?

- **Python режим** — YouTube "думает", что это обычный пользователь, не бот
- **CLI режим** — быстрее работает, не требует Python
- **Автопереключение** — если один режим не работает, пробует другой

### Как это работает?

```
1. Вставили ссылку на YouTube
2. Приложение пробует Python режим
3. Если ошибка → автоматически пробует CLI режим
4. Если оба не работают → показывает диагностику
```

---

## 🛡️ Smart blocking diagnostics

### Что это?

Умная **диагностика блокировок** — приложение автоматически определяет, *почему* YouTube заблокировал запрос, и даёт конкретные рекомендации.

### Раньше vs Сейчас

**Раньше:**
```
Ошибка: yt-dlp info failed: HTTP Error 403: Forbidden
```
*(Непонятно что делать)*

**Сейчас:**
```
⚠️ Обнаружено: Access denied (HTTP 403)

Что попробовать:
1) Использовать VPN/Proxy (SOCKS5)
2) Обновить cookies (перелогиниться на YouTube)
3) Подождать и попробовать позже

💡 Подсказка: Прокси не обнаружен. Попробуйте включить XRAY/Clash.
```

---

## 🔍 Auto-detect: 403, SABR, PO Token, DRM

### Какие блокировки распознаются?

| Код | Название | Что значит | Что делать |
|-----|----------|-----------|------------|
| **403** | HTTP Forbidden | YouTube отклонил запрос | VPN/Proxy, обновить cookies |
| **SABR** | SABR Streaming | Защита от скачивания видео | Cookies + Python режим, или MP3 |
| **PO Token** | Proof of Origin | Требуется подтверждение браузера | Cookies из залогиненного Chrome |
| **Age** | Возрастное ограничение | Видео 18+ | Cookies с авторизованного аккаунта |
| **Geo** | Географическая блокировка | Недоступно в вашей стране | VPN с другой страной |
| **Timeout** | Таймаут сети | YouTube не отвечает (soft-блок IP) | Прокси или подождать |
| **429** | Rate Limit | Слишком много запросов | Подождать 10-15 минут |
| **Bot** | Обнаружен бот | YouTube заподозрил автоматизацию | Cookies + Python режим |
| **Private** | Приватное видео | Нет доступа | Cookies с разрешённого аккаунта |
| **🔒 DRM** | DRM-защита | Контент зашифрован (Premium, Movies) | Скачивание невозможно |
| **🎫 Members** | Members-Only | Требуется подписка на канал | Cookies с аккаунта подписчика |

### Пример диагностики

Если видите ошибку с SABR:

```
⚠️ Обнаружено: SABR streaming protection active

YouTube SABR защита активна.
Что попробовать:
1) Использовать cookies из залогиненного Chrome
2) Попробовать скачать только аудио (MP3)
3) Использовать proxy/VPN
```

---

## 🔒 DRM Detection (v1.2.1)

### Что это?

**Определение DRM до скачивания** — приложение автоматически определяет, защищён ли контент DRM (Widevine, PlayReady, FairPlay), и показывает понятное сообщение вместо ошибки 403.

### Типы защиты

| Тип | Описание | Можно скачать? |
|-----|----------|---------------|
| **DRM** | Шифрование контента (YouTube Premium, Movies) | ❌ Нет |
| **Premium** | Эксклюзив YouTube Premium | ❌ Нет |
| **Members-Only** | Контент для подписчиков канала | ✅ С cookies |
| **Paid** | Платный контент (аренда/покупка) | ❌ Нет |
| **Age-Restricted** | 18+ контент | ✅ С cookies |

### Как это выглядит в UI

Вместо непонятной ошибки 403:

```
❌ Error: HTTP Error 403: Forbidden
```

Теперь показывается понятное сообщение:

```
🔒 DRM-Protected Content

This video is DRM-protected and cannot be downloaded.

✔ Available offline in YouTube app (with Premium)
✔ Can be screen-recorded
✖ Cannot be downloaded as a file

This is a content protection measure, not an error.
```

### Профессиональный подход

1. **Определяем DRM заранее** — до попытки скачивания
2. **Не "падаем" с ошибкой** — показываем информацию
3. **Предлагаем альтернативы** — офлайн в приложении, запись экрана
4. **Честно объясняем** — это защита контента, не баг

---

## 🔄 Multi-tool fallback

### Что это?

**Автоматический fallback** между инструментами:

```
yt-dlp (не работает) → lux (не работает) → you-get
```

### Как включить/выключить?

В интерфейсе:
- **Mode → Auto fallback: ON** — пробует все инструменты
- **Mode → Auto fallback: OFF** — использует только выбранный

### Когда полезно?

- **ON** — когда YouTube блокирует один инструмент, другой может работать
- **OFF** — когда знаете, что ваш инструмент работает (быстрее)

### Порядок fallback

| Выбран | Порядок попыток |
|--------|-----------------|
| yt-dlp | yt-dlp → lux → you-get |
| lux | lux → yt-dlp → you-get |
| you-get | you-get → yt-dlp → lux |

---

## 📋 Краткая шпаргалка

### Если видео не скачивается:

1. **Включите прокси** (Tools → Proxy → `socks5h://127.0.0.1:1080`)
2. **Используйте cookies** (Tools → Cookies → Chrome logged-in)
3. **Попробуйте MP3** (Quality → Audio Only)
4. **Включите Auto fallback** (Mode → Auto fallback: ON)

### Если видите конкретную ошибку:

| Ошибка | Действие |
|--------|----------|
| 403 Forbidden | Прокси + Cookies |
| SABR | MP3 или Cookies |
| PO Token | Cookies обязательно |
| Timeout | Прокси или подождать |
| Age-restricted | Залогиниться на YouTube |
| Geo-blocked | VPN другой страны |

---

## 🔧 Технические детали

### Архитектура v1.2.0

```
┌─────────────────────────────────────────┐
│         InfoExtractor Orchestrator      │
│                                         │
│   Python Mode ←──auto──→ CLI Mode       │
│       ↓                    ↓            │
│   ┌─────────────────────────────────┐   │
│   │   Blocking Diagnostics          │   │
│   │   (403, SABR, PO Token, etc.)   │   │
│   └─────────────────────────────────┘   │
│                   ↓                     │
│         Download Orchestrator           │
│   yt-dlp → lux → you-get (fallback)    │
└─────────────────────────────────────────┘
```

### Файлы модулей

```
src-tauri/src/downloader/
├── extractors/           # Извлечение информации
│   ├── python.rs         # Python режим
│   ├── cli.rs            # CLI режим
│   ├── orchestrator.rs   # Автопереключение
│   └── diagnostics.rs    # Диагностика блокировок
├── format_selector.rs    # Выбор качества
└── backends/             # Инструменты загрузки
```

---

## 📚 Связанная документация

- [YOUTUBE_BLOCKING.md](YOUTUBE_BLOCKING.md) — подробно о блокировках YouTube
- [ARCHITECTURE_2025.md](ARCHITECTURE_2025.md) — техническая архитектура
- [README.md](README.md) — быстрый старт

---

**Версия:** 1.2.1  
**Автор:** Kurein M.N.

